name: Deploy to RunPod

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: SSH into RunPod and deploy
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ssh.runpod.io
        username: k0ae2fcgvyqy7h-64411556
        key: ${{secrets.SSH_KEY}}
        port: 22
        script: |
            cd /workspace/ChatterBoxTTS
            echo "Before fetch:"
            git log -1 --oneline
            git fetch origin master
            echo "After fetch, before reset:"
            git log -1 --oneline origin/master
            git reset --hard origin/master
            echo "After reset:"
            git log -1 --oneline
            pkill -f "uvicorn main:app" || true
            nohup uvicorn main:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
       